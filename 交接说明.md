# 国外站项目交接说明

更新时间：2026-08-14

## 1. 交接包用途

这个压缩包用于把国外站项目交给新同事重新部署。

本包不包含旧海外站服务器的历史部署信息，也不包含真实密钥。

## 2. 项目结构

| 路径 | 说明 |
|---|---|
| `overseas-site/` | 国外站前端，Vue 3 + Vite |
| `overseas-api/` | 国外站后端，Rust + Axum |
| `deploy/` | 数据库初始化、上游 Key 检查、冒烟测试脚本 |
| `docker-compose-deploy.yml` | 生产 Docker Compose 部署文件 |
| `docker-compose.yml` | 本地开发数据库和 Redis 配置 |
| `.env.production.example` | 生产环境变量模板 |
| `README.md` | 项目功能、运行和测试说明 |
| `ARCHITECTURE.md` | 项目架构说明 |
| `部署说明_新服务器.md` | 新服务器重新部署步骤 |
| `环境变量填写说明.md` | 生产环境变量填写规则 |

## 3. 已排除内容

| 类型 | 说明 |
|---|---|
| 依赖目录 | `node_modules/`、Rust `target/`、`target-budget/` |
| 构建产物 | `overseas-site/dist/` |
| 本地记录 | `.git/`、`.codex-logs/` |
| 真实环境 | `.env`、`.env.production`、`token.txt` |
| 证书目录 | `ssl/` |
| 旧部署记录 | 旧海外站 IP、旧 AWS 实例、旧证书到期记录 |

## 4. 接手顺序

| 顺序 | 动作 |
|---|---|
| 1 | 阅读 `部署说明_新服务器.md` |
| 2 | 在新服务器安装 Docker 和 Docker Compose |
| 3 | 上传本包到推荐目录 `/opt/openbridge` |
| 4 | 复制 `.env.production.example` 为 `.env.production` 并填写真实值 |
| 5 | 构建前端 |
| 6 | 启动 PostgreSQL、Redis、API |
| 7 | 配置域名和 HTTPS |
| 8 | 启动 Nginx |
| 9 | 执行健康检查和冒烟测试 |

## 5. 安全说明

不要把真实 `.env.production`、私钥正文、JWT_SECRET、数据库密码、上游 API Key、Tushare Token 提交到仓库或发到公开聊天里。

本包只记录新服务器 SSH 私钥路径、指纹和文件哈希，不随附私钥文件。
