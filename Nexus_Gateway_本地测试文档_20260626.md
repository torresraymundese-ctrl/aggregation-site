# Nexus Gateway 本地测试文档

## 1. 测试地址

| 类型 | 地址 |
|---|---|
| 本机前端 | http://127.0.0.1:5301 |
| 局域网前端 | http://192.168.1.251:5302 |
| 后端健康检查 | http://127.0.0.1:8080/health |
| OpenAPI 文档 | http://127.0.0.1:5301/api/openapi.json |

## 2. 测试账号

统一密码：`NexusTest@2026`

| 角色 | 邮箱 | 用途 |
|---|---|---|
| 普通用户 1 | `test01-20260626@nexus.local` | 用户主流程测试 |
| 普通用户 2 | `test02-20260626@nexus.local` | 权限、切换账号测试 |
| 管理员 | `admin@nexus.local` | 管理端测试 |

## 3. 测试前检查

| 检查项 | 预期结果 |
|---|---|
| 打开本机前端 | 首页正常显示，积木模型背景正常 |
| 打开局域网前端 | 同一局域网设备可访问首页 |
| 打开后端健康检查 | 页面显示 `OK` |
| 打开 OpenAPI 文档 | 返回 JSON，能看到 `openapi: 3.1.0` |

## 4. 首页与公开页测试

| 步骤 | 预期结果 |
|---|---|
| 打开首页 | 顶部导航、居中文案、漂浮积木正常显示 |
| 点击中英文切换 | 文案能在中文和英文之间切换 |
| 点击顶部“接口文档” | 进入文档页 |
| 点击顶部“模型广场” | 未登录时跳转到登录页 |
| 打开 `/privacy` 和 `/terms` | 隐私政策和服务条款页面正常显示 |

## 5. 普通用户流程测试

使用账号：`test01-20260626@nexus.local`

| 步骤 | 预期结果 |
|---|---|
| 登录账号 | 登录成功，进入控制台 |
| 查看控制台 | 余额、模型数量、最近调用等模块正常显示 |
| 切换中文/英文 | 登录后页面文案能完整切换 |
| 进入“模型广场” | 能看到 17 个模型和供应商信息 |
| 进入“接口密钥” | 能看到创建 Key 表单 |
| 创建 API Key | 选择至少一个模型，例如 `gpt-4.1-mini`，创建成功后只弹出一次完整 Key |
| 刷新 Key 列表 | 列表只显示 Key 前缀，不显示完整 Key |
| 禁用 Key | Key 状态变为停用 |
| 启用 Key | Key 状态恢复启用 |
| 删除 Key | Key 从列表消失 |
| 进入“调用日志” | 页面正常显示，无报错 |
| 进入“余额与账单” | 余额和账单页面正常显示 |
| 进入“充值余额” | 套餐能显示；当前不测试真实支付 |

## 6. API 调用测试

先用普通用户创建一把 API Key，并复制完整 Key。

### 6.1 允许模型但余额不足

请求模型使用 Key 白名单里允许的模型，例如 `gpt-4.1-mini`。

预期结果：返回 `402`，错误码为 `INSUFFICIENT_BALANCE`，说明已经进入余额校验。

### 6.2 不允许模型

如果 Key 只允许 `gpt-4.1-mini`，请求 `gpt-4o-mini`。

预期结果：返回 `403`，错误码为 `MODEL_NOT_ALLOWED`，不会进入上游，也不会扣费。

### 6.3 无效 Key

使用随便写的 Key，例如 `sk-invalid`。

预期结果：返回 `401`。

## 7. 管理端测试

使用账号：`admin@nexus.local`

| 步骤 | 预期结果 |
|---|---|
| 登录管理员账号 | 登录成功 |
| 进入 `/admin` | 能看到管理端控制台 |
| 进入“用户” | 能看到用户列表 |
| 进入“订单” | 能看到订单列表 |
| 进入“调用日志” | 仍然保持管理端侧栏，不会跳回普通用户侧栏 |
| 进入“模型定价” | 能看到模型成本价、余量、最终扣费价 |
| 进入“供应商” | 能看到供应商、Key 环境变量状态和健康状态 |
| 进入“利润报表” | 能看到 30 天收入、成本、毛利 |
| 进入“风控中心” | 能看到用户风险列表 |
| 进入“审计日志” | 能看到模型定价、供应商修改等操作记录 |

## 8. 权限测试

| 步骤 | 预期结果 |
|---|---|
| 普通用户访问 `/admin` | 前端会回到控制台或后端返回无权限 |
| 未登录访问 `/admin/risk` | 跳转登录页 |
| 普通用户直接请求管理端接口 | 返回 `403` |
| 未登录直接请求管理端接口 | 返回 `401` |

## 9. 不测试的内容

| 内容 | 原因 |
|---|---|
| 真实支付充值 | 支付闭环放到最后，目前不做真实付款 |
| 真实上游模型成功调用 | 本地环境没有配置所有真实 Provider Key，主要测试路由、权限、余额和风控 |
| 生产域名 | 本文档只用于本地和局域网测试 |

## 10. 测试结果记录

| 测试人 | 日期 | 结果 | 备注 |
|---|---|---|---|
|  |  | 通过 / 不通过 |  |

