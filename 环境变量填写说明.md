# 生产环境变量填写说明

更新时间：2026-08-14

复制模板：

```bash
cp .env.production.example .env.production
```

然后填写真实值：

```bash
nano .env.production
```

## 1. 必填变量

| 变量 | 填写说明 |
|---|---|
| `POSTGRES_DB` | 生产数据库名，例如 `abroad_prod` |
| `POSTGRES_USER` | 生产数据库用户，例如 `overseas_app` |
| `POSTGRES_PASSWORD` | 强密码，不要用示例值 |
| `DATABASE_URL` | PostgreSQL 连接串，密码要和 `POSTGRES_PASSWORD` 一致 |
| `REDIS_URL` | Docker Compose 内部默认 `redis://redis:6379` |
| `JWT_SECRET` | 至少 32 字符随机字符串 |
| `JWT_EXPIRES_IN` | 建议 `7d` |
| `PAYMENT_WEBHOOK_SECRET` | 至少 32 字符随机字符串 |
| `BYOK_MASTER_KEY_B64` | 32 字节随机主密钥的 Base64 |
| `ADMIN_EMAILS` | 管理员邮箱，多个用英文逗号分隔 |
| `CORS_ALLOWED_ORIGINS` | 正式前端 HTTPS 域名 |
| `TRUST_PROXY_HEADERS` | Nginx 反代后方部署时填 `true` |
| `PORT` | 默认 `8080` |
| `RUST_ENV` | 生产填 `production` |
| `LOG_LEVEL` | 建议 `info` |

生成随机值：

```bash
openssl rand -hex 32
openssl rand -base64 32
```

## 2. 上游模型 Key

按实际要启用的供应商填写。

| 变量 | 说明 |
|---|---|
| `PROVIDER_OPENAI_API_KEY` | OpenAI |
| `PROVIDER_ANTHROPIC_API_KEY` | Anthropic Claude |
| `PROVIDER_GOOGLE_API_KEY` | Google Gemini |
| `PROVIDER_VOLCENGINE_API_KEY` | 火山引擎 |
| `PROVIDER_DEEPSEEK_API_KEY` | DeepSeek |
| `PROVIDER_ZHIPU_API_KEY` | 智谱 |
| `PROVIDER_QWEN_API_KEY` | 通义千问 |
| `PROVIDER_MOONSHOT_API_KEY` | Moonshot/Kimi |
| `PROVIDER_MINIMAX_API_KEY` | MiniMax |
| `PROVIDER_STEPFUN_API_KEY` | 阶跃星辰 |

## 3. 异步任务

如果启用图片、音频、视频或 Replicate 类异步任务，填写：

| 变量 | 说明 |
|---|---|
| `ASYNC_WEBHOOK_PUBLIC_BASE_URL` | 公开 HTTPS API 根路径，例如 `https://你的域名/api` |
| `ASYNC_TASK_TTL_HOURS` | 异步任务保留时间，默认 `168` |

## 4. 不要写进交接包的内容

| 敏感内容 |
|---|
| 私钥正文 |
| `.env.production` |
| `JWT_SECRET` |
| 数据库密码 |
| 上游 API Key |
| Tushare Token |
| 任何真实支付密钥 |

真实 `.env.production` 只保存在服务器上。
