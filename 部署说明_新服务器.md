# 国外站新服务器部署说明

更新时间：2026-08-14

## 1. 新服务器信息

这是重新部署目标服务器，不是旧海外站服务器。

| 项 | 信息 |
|---|---|
| 新服务器 IP | `47.99.157.215` |
| SSH 用户 | `root` |
| SSH 私钥路径 | `D:\zero-q.pem` |
| SSH 命令 | `ssh -i D:\zero-q.pem root@47.99.157.215` |
| 私钥指纹 | `2048 SHA256:3HQKEps6m+Im/KplSMyrJ2QZBpxM9YveldiKxo70JKE no comment (RSA)` |
| 私钥文件 SHA256 | `ADB2C5C7AF4359AE9386E6319DCEEC2A2397AE7DD5EEABD5498DF3DD65158407` |
| 原项目云端目录 | `/opt/zeroquant2` |
| 原项目部署目录 | `/opt/zeroquant2/deploy` |
| 原项目大小 | `663M` |
| Docker 网络 | `deploy_zq2-network` |
| PostgreSQL 数据卷 | `deploy_pg_data` |
| Redis 数据卷 | `deploy_redis_data` |

## 2. 当前服务器空闲状态

旧系统已经暂停，端口已经释放。

| 容器 | 当前状态 |
|---|---|
| `zq2-nginx` | 已停止 |
| `zq2-quant-nginx` | 已停止 |
| `zq2-backend` | 已停止 |
| `zq2-data` | 已停止 |
| `zq2-postgres` | 已停止 |
| `zq2-redis` | 已停止 |

| 端口 | 状态 |
|---|---|
| `80` | 已释放，不再监听 |
| `443` | 已释放，不再监听 |
| `18080` | 已释放，不再监听 |
| `8080/8081/5432/6379` | 已释放，不再监听 |

## 3. 服务器资源快照

| 项 | 信息 |
|---|---|
| 根分区 | 40G |
| 已用 | 16G |
| 可用 | 23G |
| 使用率 | 41% |
| 内存 | 3.4Gi |
| 可用内存 | 约 2.4Gi |
| Swap | 无 |

## 4. 推荐部署目录

建议把国外站部署到独立目录，避免和旧系统目录混在一起。

```bash
mkdir -p /opt/openbridge
```

把交接包上传并解压到：

```bash
/opt/openbridge
```

旧系统目录 `/opt/zeroquant2` 和数据卷不要删除，除非确认不再需要恢复旧系统。

## 5. 安装基础软件

```bash
apt update
apt install -y ca-certificates curl gnupg git
install -m 0755 -d /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg -o /etc/apt/keyrings/docker.asc
chmod a+r /etc/apt/keyrings/docker.asc
echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.asc] https://download.docker.com/linux/ubuntu $(. /etc/os-release && echo "$VERSION_CODENAME") stable" > /etc/apt/sources.list.d/docker.list
apt update
apt install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
docker --version
docker compose version
```

## 6. 上传项目

本机执行：

```powershell
scp -i D:\zero-q.pem .\overseas-handoff.zip root@47.99.157.215:/opt/
```

服务器执行：

```bash
cd /opt
mkdir -p /opt/openbridge
unzip overseas-handoff.zip -d /opt/openbridge
cd /opt/openbridge
```

如果 zip 解压后多了一层目录，就进入实际项目目录，目录里应该能看到 `docker-compose-deploy.yml`。

## 7. 填写生产环境变量

```bash
cd /opt/openbridge
cp .env.production.example .env.production
nano .env.production
```

填写规则见 `环境变量填写说明.md`。

## 8. 构建前端

```bash
cd /opt/openbridge/overseas-site
npm ci
npm run build
cd /opt/openbridge
```

## 9. 启动后端依赖和 API

```bash
cd /opt/openbridge
docker compose --env-file .env.production -f docker-compose-deploy.yml up -d --build postgres redis api
docker compose -f docker-compose-deploy.yml ps
docker compose -f docker-compose-deploy.yml logs -f api
```

健康检查：

```bash
curl http://127.0.0.1:8080/health
```

## 10. 配置域名和 HTTPS

先把域名 A 记录解析到：

```text
47.99.157.215
```

如果使用 Cloudflare，SSL 模式建议使用 `Full (strict)`。

首次签发证书前，确认 `80` 端口开放。

```bash
docker compose --env-file .env.production -f docker-compose-deploy.yml stop nginx || true
mkdir -p /etc/letsencrypt /var/lib/letsencrypt /var/www/certbot
docker run --rm -p 80:80 \
  -v /etc/letsencrypt:/etc/letsencrypt \
  -v /var/lib/letsencrypt:/var/lib/letsencrypt \
  certbot/certbot certonly --standalone \
  --non-interactive --agree-tos \
  -m 你的邮箱 \
  -d 你的域名 \
  -d www.你的域名
```

签发成功后，按实际域名修改：

```text
overseas-site/nginx.conf
```

需要改的地方：

| 配置 | 要改成 |
|---|---|
| `server_name` | 新域名和 `www` 域名 |
| `ssl_certificate` | 新证书目录 |
| `ssl_certificate_key` | 新证书目录 |

## 11. 启动完整服务

```bash
cd /opt/openbridge
docker compose --env-file .env.production -f docker-compose-deploy.yml up -d --build
docker compose -f docker-compose-deploy.yml ps
```

## 12. 部署后检查

| 检查 | 命令 |
|---|---|
| 首页 | `curl -I https://你的域名` |
| API 健康 | `curl https://你的域名/api/health` |
| 模型列表 | `curl https://你的域名/api/models` |
| 容器状态 | `docker compose -f docker-compose-deploy.yml ps` |
| API 日志 | `docker compose -f docker-compose-deploy.yml logs -f api` |
| Nginx 日志 | `docker compose -f docker-compose-deploy.yml logs -f nginx` |

## 13. 旧系统恢复命令

仅备查。恢复旧系统前，先确认国外站没有占用同样端口。

```bash
cd /opt/zeroquant2/deploy
docker compose up -d postgres redis backend data-service
docker compose -f docker-compose.quant-nginx.yml up -d quant-nginx
docker start zq2-nginx
```

旧系统停机前健康检查曾通过：`/` 返回 200，`/api/v1/health` 正常，`/data/v1/health` 正常；数据卷没有删除。
